1. What was the most confusing thing about Python compared to JavaScript?
Indentation is importnat in python. JS is not like that.
2. What does an HTTP status code tell you? Give one example.
The HTTP status code tell me the result of an HTTP request.
3. What was the difference between a path parameter and a query parameter?
Path parameter is part of the URL path, it is for identify specific resource, where as query parameter comes after ? in the URL, used for filtering or options.
4. What would happen to all the data if you restarted the server right now?
All data will disappears.
Why is that a problem, and what will we use to fix it?
users would lose their books whenever they restart. To fix it we can use a database, so it would save data to disk and it survives restarts. 